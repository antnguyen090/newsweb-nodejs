<% 

    showCategory = (collection, id) => {
        if (collection == null) return
        let result = collection.find(obj => {
            return obj._id == id
          }).name
        return result
    }
%>
