<%- include('./showCategoryName'); -%>
<%- include('./customdate'); -%>

<%
    showDetail = (data,category, articlePhoto) => {
        return `<div class="position-relative mb-3">
            <img class="img-fluid w-100" src="/uploads/${articlePhoto}/${data.thumb}" style="object-fit: cover;">
                <div class="bg-white border border-top-0 p-4">
                    <div class="mb-3">
                        <a class="badge badge-primary text-uppercase font-weight-semi-bold p-2 mr-2" href="category/${showCategoryName(category, data.categoryId).slug}">${showCategoryName(category, data.categoryId).name}</a>
                        <a class="text-body" href="">${customDate(data.createdAt)}</a>
                    </div>
                    <h1 class="mb-3 text-secondary text-uppercase font-weight-bold">${data.name}</h1>
                    ${data.editordata}
                </div>
                 <div class="d-flex justify-content-between bg-white border border-top-0 p-4">
                            <div class="d-flex align-items-center">
                                <img class="rounded-circle mr-2" src="/frontend/dist/img/user.jpg" width="25" height="25" alt="">
                                    <span>John Doe</span>
                                </div>
                                <div class="d-flex align-items-center">
                                    <span class="ml-3">
                                        <i class="far fa-eye mr-2"></i>12345</span>
                                    <span class="ml-3">
                                        <i class="far fa-comment mr-2"></i>123</span>
                                </div>
                </div>
        </div>`
    }
%>
